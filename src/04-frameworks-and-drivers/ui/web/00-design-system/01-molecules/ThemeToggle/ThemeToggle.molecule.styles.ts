import { css } from '@emotion/react';

// Hiện tại ThemeToggle tái sử dụng hoàn toàn style của Button Atom (variant="ghost", size="icon").
// File này giữ chỗ để custom style đặc thù cho ThemeToggle nếu cần trong tương lai.
export const themeToggleStyles = css``;